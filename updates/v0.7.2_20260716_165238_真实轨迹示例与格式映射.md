# v0.7.2 真实轨迹示例与格式映射

- 版本：v0.7.2
- 日期：2026-07-16
- 更新类型：技术材料、真实数据示例、文档
- 状态：已完成

## 一、更新概述

从 `train_candidates_16k_2028.jsonl` 对应的 27 条 SQL 强验证子集中选择一条适合讲解的短轨迹，通过规划元数据追溯其原始 OpenAI-style 记录，并补充到 `docs/trajectory_sft_process.md`。

## 二、格式结论

- 2,028 条合并文件本身是 ms-swift agent JSONL，不是原始 OpenAI 格式。
- 其轨迹均可通过 `(source_file, source_line)` 追溯到原始 OpenAI-style `messages`。
- 材料展示原始 OpenAI 协议，另用映射表说明它如何转换成 ms-swift 训练消息。
- `reasoning_content` 是项目扩展字段；`assistant.tool_calls`、`role=tool` 和 `tool_call_id` 沿用 OpenAI-style 工具调用结构。

## 三、所选样本

| 字段 | 值 |
|---|---|
| source | `qwen3.6-27B_20260628_v41_openai.jsonl:199` |
| task_id | `task_000201` |
| split | train |
| phase | core_8k |
| quality tier | `sql_result_verified` |
| 输入长度 | 3,464 tokens |
| 监督长度 | 416 tokens |
| 完整工具调用 | 4 次 |
| 查询结果 | 6,639 分钟 |

样本通过 SQLite 表结构检查后执行：

```sql
SELECT SUM(dwell_minutes)
FROM fact_waybill_event
WHERE date(event_time) = '2026-06-19';
```

工具返回 `6639`，最终回答给出 `6639 分钟`。材料为控制篇幅省略目录枚举和数据表列表，但保留关键表结构、SQL、工具结果和答案，并明确标注这是节选。

## 四、新增说明

1. 给出该真实轨迹的 OpenAI-style JSON 节选。
2. 逐消息标注 system/user/assistant/tool 是否参与 loss。
3. 解释生成 SQL 时不能看到未来结果，而生成最终答案时可以看到过去 observation。
4. 增加 OpenAI 原始字段到 ms-swift `assistant/tool_call/tool_response` 的对应表。
5. 明确原始记录没有顶层工具 schema，训练转换时才补齐 bash/read/write/edit schema。

## 五、验证与边界

- 通过规划文件确认样本属于 16K 内 train 和 `sql_result_verified`。
- 通过原始源文件定位核对用户问题、表结构、SQL、工具结果和最终答案。
- 材料中的两个 JSON 代码块均通过 `json.loads` 解析；项目测试 `27 passed`。
- 未将完整原始数据文件提交 GitHub；文档只保存必要的真实节选。
- 示例用于解释格式和训练机制，不代表 2,028 条候选均已达到相同正确性等级；其中仍有 2,001 条待复核。

## 六、兼容性与回滚

本轮只修改说明材料、README 和版本报告，不修改原始数据、2,028 条候选文件、训练配置、模型或服务器产物。回滚只需移除新增文档段落和版本记录。
