---
版本: v0.1.0
报告日期: 2026-07-13
更新类型: 数据集重组 + 服务器同步 + 商业模型数据推送
更新人: renjunxiang（Claude 协助）
状态: 已完成，本机服务器一致
---

# 数据集重组与服务器同步（open_source + sandboxes 两仓分立）

## 一、更新概述

按"开源数据 + 沙箱数据"两仓分立设计重组 `datasets/`，本地 + 服务器同步执行：开源 benchmark 归 `open_source/`，沙箱（含 149 个扁平化版 + 商业模型轨迹转换 SFT）归 `sandboxes/`；同步迁移 22 处脚本路径引用；服务器 sandbox 污染文件清理；商业模型轨迹产物（SFT 数据 + gzip 归档 + verdict）推送服务器。**本机和服务器存储结构一致**，为训练前按模型表现定配比铺路。

## 二、操作清单

### 1. 本机 datasets/ 重组

```
datasets/
├── open_source/              ← 开源数据分仓（原 raw + sft 整合）
│   ├── raw/                  32GB（28 个 benchmark：AIME24/BBB/C-Eval/MATH...）
│   └── sft/                  8GB（processed/{with,without}_golden_thinking + final/）
├── sandboxes/                ← 沙箱数据分仓（新建）
│   ├── raw/                   3.3GB（149 个扁平化沙箱）
│   │   ├── sft/  19 个
│   │   ├── dpo/  100 个
│   │   ├── dev/  20 个
│   │   └── test/ 10 个
│   └── sft_data/              957MB（5 个商业模型轨迹转换 SFT jsonl）
└── （cpt/dpo/rl 空占位已删）
```

- `mv datasets/raw datasets/open_source/raw`（32GB 同盘秒级）
- `mv datasets/sft datasets/open_source/sft`（8GB 同盘秒级）
- `mv datasets/sft/raw/commercial_models/* datasets/sandboxes/sft_data/`（5 个 jsonl 从开源挪到沙箱分仓——源头是沙箱不是开源）
- 删空 `datasets/cpt/dpo/rl`（含空 final/processed 子目录，未准备好不占位）

### 2. 22 处脚本路径迁移

`datasets/sft` → `datasets/open_source/sft`，`datasets/raw` → `datasets/open_source/raw`。涉及：
- 4 个 train yaml（`qwen3_5_27B_sft.yaml` `qwen3_6_27B_lora*.yaml` `qwen3_6_27B_sft.yaml`）绝对路径
- `sft_lora_qwen35.py` `merge.py` BASE 常量
- 2 个 benchmark_config yaml + `inference_config.yaml`
- `dashboard/serve.py` `sync_dataset_counts.py` `tests/test_serve.py`
- `run_benchmark.py` `to_sharegpt.py` `to_openai.py` `convert.py` 等

grep 验证：49 处引用 0 残留，包路径 `mindspeed_mm/.../huggingface` 未误伤。

### 3. 服务器最小同步（task #12）

服务器 `/data/renjunxiang/coding/huawei_train/` 是**老版扁平布局**（`train/` `data/` `eval/` `utils/` `web/` 在根，`serve.py` 在根），本地是**新版模块化**（`scripts/train/` `dashboard/serve.py`）。服务器非 git 仓库（rsync 同步过来的）。

- 服务器 `mv datasets/sft datasets/open_source/sft`（保留原有 final 多出的 `16samples/8samples.jsonl` 测试文件）
- rsync `configs/` `scripts/` `dashboard/` 多源同步到服务器根（rsync 多源 + 目标带 / 会合并内容到根同名目录，等价于按服务器老布局同步）
- 补同步 `configs/`（rsync 多源没保留 configs/ 前缀，导致服务器 `configs/eval/benchmark_config.yaml` 还是老路径，单独 rsync configs/ 修复）
- 验证服务器关键文件全部新路径：`train/qwen3_6_27B_sft.yaml`(2 处) `configs/eval/benchmark_config.yaml`(4 处) `serve.py`(1 处) `data/merge.py`(3 处) `inference_config.yaml`(1 处)

### 4. 服务器 sandbox 污染清理（task #13）

服务器 `/data/renjunxiang/pi/sandbox/` 根目录混了 6 月 24 日测试 task 产出 + npm 依赖 + sft 备份：
- 5 个业务分析报告 .md + logistics.sqlite + test_tasks.jsonl（report_generation 测试产出）
- node_modules/ + package.json + package-lock.json（npm sqlite3 依赖）
- sft.bak.20260707_204043/（7 月 3 日 sft 备份）

全部 mv 到 `/data/renjunxiang/pi/sandbox/_junk/`。清理后 sandbox 根目录干净，只剩 `sft/ dpo/ dev/ test/` 四组沙箱 + `_junk/`。

### 5. 149 个扁平化沙箱拉回本地（task #11）

服务器 `/data/renjunxiang/pi/sandbox/{sft,dpo,dev,test}/` 是**已扁平化版**（logistics.sqlite + dwh_tasks.jsonl 在根），4 次 rsync 串联拉回本地 `datasets/sandboxes/raw/{sft,dpo,dev,test}/`：

| 组 | 数量 | 体积 |
|---|---|---|
| sft | 19 个 | 2.1GB |
| dpo | 100 个 | 892MB |
| dev | 20 个 | 165MB |
| test | 10 个 | 81MB |
| **合计** | **149 个** | **3.3GB** |

抽查 `sft/20260628_v15` + `dpo/20260623_v6` 均扁平化结构 ✓。

### 6. 商业模型数据推送服务器

| 产物 | 路径 | 大小 | 服务器状态 |
|---|---|---|---|
| openai 训练数据（5 jsonl）| `datasets/sandboxes/sft_data/` | 957MB | ✓ 推送 |
| gzip 归档（5 tar.gz）| `archives/` | 4.7GB | ✓ 推送 |
| verdict（8 jsonl）| `eval_results/verdicts/` | 4.9MB | ✓ 推送 |

商业模型轨迹转换的 SFT 数据（deepseek_v15/glm52_v15/qwen37max_v15/v20/v21）+ gzip 归档备份 + verdict 判分结果，本机服务器一致。

### 7. 流程脚本固化（task #14）

`/tmp/` 下 4 个轨迹瘦身流程脚本复制到 `scripts/data/` + commit：
- `run_verdict_batch.py`：批量 verdict 判分
- `run_openai_batch.py`：批量 openai 格式转换
- `run_archive_batch.py`：批量 gzip 归档 + 验证 + 删原始
- `wait_and_archive.sh`：监控 verdict+openai 完成后自动跑 gzip 归档

复用场景：deepseek_v20/glm52_v20 跑完后同样流程瘦身。

## 三、commit 记录

| commit | 说明 |
|---|---|
| `c684eb0` | feat: 看板模型评测页升级 + 商业模型轨迹跑批脚本 + 阶段报告（28 文件 / +3635 / -179）|
| `833caa4` | refactor(datasets): 路径迁移 datasets/{sft,raw} → datasets/open_source/{sft,raw}（22 文件 / +328 / -199）|
| `9f8c83c` | feat(data): 固化轨迹瘦身流程脚本到 scripts/data/（4 文件 / +158）|

## 四、验证结果

- **本地 datasets/** 结构完整：open_source/{raw, sft} + sandboxes/{raw/{sft,dpo,dev,test}, sft_data}
- **服务器 datasets/** 结构同步：open_source/sft + sandboxes/sft_data（服务器不需要 raw benchmark + sandboxes/raw，因为是训练机不是开发机）
- **路径引用 0 残留**：grep `datasets/sft` `datasets/raw` 全空（除 `datasets/open_source/` 前缀）
- **服务器关键文件全部新路径**：train/configs/serve.py/data/inference_config 全部含 `datasets/open_source/`
- **服务器 sandbox 根干净**：只剩 sft/dpo/dev/test + _junk
- **149 沙箱拉回本地**：3.3GB，抽查扁平化结构正确
- **商业模型产物本机服务器一致**：sft_data(957MB) + archives(4.7GB) + verdicts(4.9MB)

## 五、涉及文件

**本机**：
- `datasets/` 重组（raw/sft → open_source/raw|sft；新建 sandboxes/raw/{sft,dpo,dev,test} + sft_data）
- 22 个脚本/yaml/报告路径迁移
- `scripts/data/run_*.py` + `wait_and_archive.sh`（4 个流程脚本固化）
- `.gitignore`（加 `archives/`）

**服务器**：
- `/data/renjunxiang/coding/huawei_train/datasets/` 重组（open_source/sft + sandboxes/sft_data）
- `/data/renjunxiang/coding/huawei_train/{train,data,eval,configs,utils,web,tests,serve.py,sync_*.py,inference_config.yaml}` rsync 同步代码改动
- `/data/renjunxiang/coding/huawei_train/archives/` 5 个 tar.gz
- `/data/renjunxiang/coding/huawei_train/eval_results/verdicts/` 8 个 jsonl
- `/data/renjunxiang/pi/sandbox/_junk/` 污染文件归集

## 六、遗留问题

1. **服务器 `scripts/` 目录历史副本**：服务器根有 `scripts/`（含 data/eval/inference/train 子目录），是早期历史副本含老路径文件。服务器实际运行用根的 `train/` `data/` `eval/` 路径（已同步新路径），`scripts/` 不参与运行。要清理 or 保留待定。
2. **看板 `--sandbox-source` 默认值**：`dashboard/serve.py` 默认指向 `/Users/renjunxiang/coding/sf_my_sandbox/output`（sf 嵌套 306MB 版），本地已有 `datasets/sandboxes/raw/`（扁平 38MB 版）。改不改默认指向本地待定。
3. **未提交改动**：commit 后 git status 又冒出新改动（configs/eval/benchmark_config.yaml + dashboard/serve.py/web/* + eval_results/models/qwen3.5-2b.yaml + scripts/eval/vllm_client.py + 删 qwen36-27b-base/sft-reasoning.yaml + 轨迹瘦身 update_report），不是本次工作做的，待用户决定是否 commit。
4. **deepseek_v20 + glm52_v20 跑批中**：未做轨迹瘦身（verdict + openai + gzip 归档），跑完后复用 `scripts/data/run_*.py` + `wait_and_archive.sh` 同样流程处理。
5. **149 沙箱抽取脚本未固化**：服务器扁平化版是当时 claudecode 一次性手动处理上传的，没固化成"从 sf 嵌套版 → 扁平化版"的抽取脚本。如果要可溯源复现，需要补写抽取脚本作为备份方案（主路径仍是从服务器 scp 已扁平化版）。
